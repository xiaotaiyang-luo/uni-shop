实现分类页面的步骤分析
1.创建cate分支
	在cate分支下进行页面的开发
2.先渲染出cate页面的基本结构，便于后期进行动态的渲染
布局分析：
	页面由一个大盒子包含两个主体内容盒子，大盒子内一个为左侧的一级分类，另一个为右侧的二级以及三级分类
实现滑动功能分析：
	手机上要进行上下滑动，所以需要用到scroll标签，因为是上下滑动，所以指定为scroll-y，并给一个指定的高度，
细节：
	注意：右侧一级分类的宽度是固定的，所以宽度可以不用rpx单位，直接使用px。其内部的元素的单位也一样
	因为需要将内容高度占满整个可操作的范围，所以需要使用到
3.获取动态渲染cate页面需要的数据，
	发起网络数据请求获取，并将数据转存到页面的data中去
4.